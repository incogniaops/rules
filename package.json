{
  "name": "rules",
  "private": true,
  "version": "0.0.0",
  "description": "Reglas y filosofía profesional y académica (.md) con lint y verificación de enlaces",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "lint:md": "markdownlint-cli2 \"**/*.md\" \"#node_modules\"",
    "lint:md:fix": "markdownlint-cli2 --fix \"**/*.md\" \"#node_modules\"",
    "check:links": "find . -type f -name '*.md' -not -path './node_modules/*' -not -path './.git/*' -print0 | xargs -0 -r markdown-link-check -q -c .markdownlinkcheck.json"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.13.0",
    "markdown-link-check": "^3.12.2"
  }
}
